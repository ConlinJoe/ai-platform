@AGENTS.md

Canonical skills: `.agents/skills/`. Claude Code also loads
`.claude/skills/`, which bootstrap links to the same directories.

Do not duplicate `.cursor/rules/` here. Read matching Cursor rules when
the files in play match (`core/agent-core.md` tables).
